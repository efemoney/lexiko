plugins {
  com.android.library
  id("convention-android")
}

androidComponents {
  // beforeVariants(
  //   selector = selector().withBuildType("release"),
  //   callback = { it.enabled = false }
  // )
}
